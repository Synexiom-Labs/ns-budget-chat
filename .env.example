# =============================================
# NS Budget Chat — Environment Variables
# Copy this file to .env.local and fill in values
# =============================================

# --- Required: Anthropic ---
ANTHROPIC_API_KEY=sk-ant-...

# --- Required: Voyage AI (embeddings) ---
# Sign up at https://www.voyageai.com — free tier available
VOYAGE_API_KEY=pa-...

# --- Required: Pinecone (vector store) ---
# Sign up at https://www.pinecone.io — free serverless tier
PINECONE_API_KEY=pcsk_...
PINECONE_INDEX=ns-budget-chat

# --- Optional: Upstash Redis (rate limiting + caching) ---
# Sign up at https://upstash.com — free tier available
# Without this, rate limiting is disabled and responses are not cached
UPSTASH_REDIS_REST_URL=https://...upstash.io
UPSTASH_REDIS_REST_TOKEN=...

# --- Optional: App config (shown values are defaults) ---
NEXT_PUBLIC_APP_URL=http://localhost:3000
RATE_LIMIT_RPM=20
MAX_OUTPUT_TOKENS=1500
CACHE_TTL_HOURS=168
